# Client-Side Modifications for Property Data Fetching

This document outlines the files and folders that were analyzed and modified on the client-side to resolve the data fetching and display issues.

| File/Folder | Activity | Explanation | Before | After |
| --- | --- | --- | --- | --- |
| `Client/src/context/CatalogContext/index.tsx` | Analyzed | Examined to understand how catalog data is managed. | N/A | N/A |
| `Client/src/modules/pages/catalog/components/CatalogList/index.tsx` | Analyzed | Examined to understand how data is fetched and rendered. | N/A | N/A |
| `Client/src/hooks/index.ts` | Analyzed | Examined to locate the `useDataFetching` hook. | N/A | N/A |
| `Client/src/types/data.ts` | Analyzed | This file was analyzed to understand the `ICatalogData` and `ICatalogTable` types. | N/A | N/A |
| `Client/src/hooks/useDataFetching.tsx` | Modified | **Data Transformation:** This file is responsible for fetching property data from the server. It has been modified multiple times to correctly transform the data from the server's format to the format expected by the client application, including fixing type errors and correctly mapping filterable fields. | `const address = property.address as { street?: string; city?: string } \|\| {}; const specifications = property.specifications as Record<string, unknown> \|\| {};` | `const address = typeof property.address === 'string' ? JSON.parse(property.address) : property.address \|\| {}; const specifications = typeof property.specifications === 'string' ? JSON.parse(property.specifications) : property.specifications \|\| {};` |
| `Client/src/hooks/useDataFetching.tsx` | Modified | **Removed Visibility Filter:** The visibility filter was removed to ensure all properties are passed to the `CatalogList` component. | `.filter((item) => item.visibility)` | (line removed) |
| `Client/src/utils/filters.ts` | Modified | **Updated Initial Filters:** Changed the initial value of `contractType` to `allValue` to ensure all properties are displayed by default. | `contractType: 'Оренда',` | `contractType: allValue,` |
| `Client/src/modules/pages/catalog/components/CatalogList/index.tsx` | Modified | **Updated Filtering Logic:** Modified the filtering logic to correctly handle the "All" case for `contractType`. | `filters.contractType.toLowerCase() === item.contractType.toLowerCase() &&` | `(filters.contractType === all \|\| filters.contractType.toLowerCase() === item.contractType.toLowerCase()) &&` |
| `Client/src/modules/pages/catalogPage/utils/units.ts` | Modified | **Added Indonesian Language:** Added an entry for the Indonesian language to the `UNITS` object to resolve a `TypeError`. | (no 'id' property) | `id: { pieces: 'buah', landPlot: 'are', squareMeters: 'm²', month: 'per bulan', currency: 'rp', priceFrom: 'dari ', priceTo: 'ke ', separator: ' atau ', },` |
| `Client/src/hooks/usePropertyPhoto.tsx` | Modified | **Updated ID Type:** Changed the `id` parameter from a `number` to a `string` to correctly handle the UUID from the database. | `const usePropertyPhoto = (id: number): IGalleryList[] => {` | `const usePropertyPhoto = (id: string): IGalleryList[] => {` |
| `Client/src/modules/pages/catalog/components/CatalogCard/index.tsx` | Modified | **Updated ID Type:** Changed the `id` to a string when calling the `usePropertyPhoto` hook. | `usePropertyPhoto(id)` | `usePropertyPhoto(id.toString())` |
| `Client/src/modules/common/components/DefaultPoster/index.tsx` | Modified | **Updated Default Poster:** Changed the default poster to display the image from `/assets/logo.png`. | `<img src="/assets/logo 2.png" alt="Default Poster" />` | `<img src="/assets/logo.png" alt="Default Poster" />` |